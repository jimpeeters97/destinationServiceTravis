FROM openjdk:8-jdk-alpine
RUN apk update && apk upgrade && apk add netcat-openbsd
RUN mkdir -p /usr/local/les10TaakDestinationService
ADD les10TaakDestinationService-0.1.0.jar /usr/local/les10TaakDestinationService
ADD run.sh run.sh
RUN chmod +x run.sh
CMD ./run.sh