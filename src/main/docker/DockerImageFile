FROM openjdk:8-jdk-alpine
RUN apk update && apk upgrade && apk add netcat-openbsd
RUN mkdir -p /usr/local/les6TaakDestinationService
ADD les6TaakDestinationService-0.1.0.jar /usr/local/les6TaakDestinationService
ADD run.sh run.sh
RUN chmod +x run.sh
CMD ./run.sh